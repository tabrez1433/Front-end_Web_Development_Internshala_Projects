*** Project's Summary ***

Name: Md Yunus Tabrez
Batch: 1st November 2023
Website/title: Food Zone
Assingment: 2 (Styling Through CSS)

Welcome to the Food Zone Website project! This project aims to create a visually appealing and user-friendly website for a fictitious restaurant or food delivery service. The website is built using HTML and CSS only.

Features:

1. Homepage(Food Zone): An attractive landing page that showcases the restaurant's features & services.

2. Our Menu Page: A dedicated menu page with clear sections for Breakfast, Lunch and Dinner. Each menu item is presented with a mouth-watering image and name.

3. Contact Us: A contact form and essential contact details (phone number, email) for customers to reach out. This enhances customer engagement and makes it easy for them to inquire or provide feedback.

4. About Us: A section providing information about the restaurant's history, mission, and values. This adds a personal touch to the website and helps build a connection with customers.

[note: Currently!, all the above sections have dummy data.] 


*** Tags and Attributes used ***

On code, I have commented regarding the tags and attribute uses for better go through.

I have used these:

* <head> tag to define the head portion of the document which contains information related to the document or metadata(information about data).
* <title> tag to label the title of web pages.
* <link> tag to attach other/external file with the HTML document file.
* <div> tag to define a division or a section in an HTML document.
* <span> tag is a generic inline container element, to wrap sections of text for styling purposes or to add attributes to a section of text without creating a new line of content.
* <img> tag used to integrate/embed an image in an HTML page.
* src attributes is required, and contains the path to the image, want to embed.
* alt attribute defines an alternative text description of the image.
* <a> anchor tag to create routes or hyperlink between current page to another pages.
* href attribute specifies the URL of the page the link goes to, without href <a> tag will not be hyperlink.
* <button> tag is used to create a clickable button within HTML document file.
* Heading tags (<h1> - <h6>) for define HTML headings. Headings are the titles or the subtitles of the content that we want to display on the web page.
* <form> tag used to create forms and used with the accept, action and other attributes.
* <br> tag to insert a single line break
* <p> tag is used to define paragraph of the webpage
* The @import rule  used to import one style sheet into another style sheet.
* The CSS :root pseudo-class selector is used to select the highest-level parent of a given specification.
* Universal Selector in CSS is used to select all the elements on the HTML page.
* The box-sizing property allows us to include the padding and border in an element's total width and height.
* CSS gradients let you display smooth transitions between two or more specified colors.
* CSS grid layout introduces a two-dimensional grid system to CSS. Grids can be used to lay out major page areas or small user interface elements.
* The Flexible Box Layout Module, makes it easier to design flexible responsive layout structure without using float or positioning.
* The CSS position property defines the position of an element in a document. This property works with the left, right, top, bottom and z-index properties to determine the final position of an element on a page. There are five values the position property can take.
* CSS display is the most important property of CSS which is used to control the layout of the element. It specifies how the element is displayed.
* The :hover selector CSS pseudo-class is used to style elements when the mouse hovers over them. It can be used on every element.
* CSS transitions allows you to change property values smoothly, over a given duration.


